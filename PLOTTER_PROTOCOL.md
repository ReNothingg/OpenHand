Последовательный порт: 8 бит данных, 1 стоп-бит, без чётности (`8N1`).
Исходная программа предлагает 9600, 115200 и 250000 бод; значение по умолчанию — 115200.
Используются RTS и DTR.
GRBL и EBB при подключении получают байт `0x18` (Ctrl-X), Marlin — команду `M115`.
OpenHand отправляет следующую команду только после ответа `ok`/`OK`. Ошибки `error` и `ALARM` останавливают задание.
Окончание строки: `CRLF`.
Перемещение: `XM,<длительность мс>,<шаги X>,<шаги Y>`.
Перо вверх: `SP,1,<задержка мс>`.
Перо вниз: `SP,0,<задержка мс>`.
Остановка/сброс: `R`.
В исходной конфигурации: 100 шагов/мм, предел скорости 25 000 шагов/с.
Окончание строки: `CRLF`.
Абсолютные миллиметры: `G21`, `G90`.
Холостой ход: `G0 X… Y… F…`.
Рисование: `G1 X… Y… F…`.
Сервопривод: `M3 S<позиция>`.
Ось Z: `G1 G90 Z… F…`.
Лазер/PWM: `M3 S…` и `M5`.
Ручное относительное движение GRBL 1.1: `$J=G21G91X…Y…F…`.
Установка текущей точки как нуля: `G10 P0 L20 X0 Y0 Z0`.
Пауза/продолжение: realtime-байты `!` и `~`; аварийный сброс — Ctrl-X.
Окончание строки: `LF`.
Идентификация: `M115`.
Сервопривод: `M280 P0 S<позиция>`.
Перо на оси Z или E: `G1 G90 Z… F…` / `G1 G90 E… F…`.
Относительный jog: `G91`, затем `G0 X… Y… F…`.
Установка нуля: `G92 X0 Y0 Z0`.
Аварийная остановка движения: `M410`.
`.gfont` — ZIP-контейнер с собственным префиксом метаданных. Имена ZIP-записей — десятичные Unicode-коды символов. Содержимое одной глифы записано в big-endian порядке:
1. `uint16` — код символа;
2. `uint32` — количество `float32`;
3. пары `float32 x, y`;
4. `uint32` — количество флагов;
5. байты флагов, по одному на точку: `0` начинает новый штрих с поднятым пером, `1` продолжает линию с опущенным пером.
В OpenHand есть собственный браузерный парсер без загрузки шрифта на сервер. В окне плоттера доступны восемь встроенных вариантов:
- пять рукописных вариантов `Если Мечта · Unicode` с 12 806 символами;
- три технических варианта `ISO 3098` с полной русской кириллицей и основными знаками.
Любой другой `.gfont` по-прежнему можно загрузить вручную. Для открытых однолинейных шрифтов LibreCAD добавлен конвертер:
```powershell
python scripts/lff_to_gfont.py input.lff font/plotter/output.gfont
```
Все рабочие файлы встроенных плоттерных шрифтов и сведения об их лицензиях находятся в `font/plotter`.
Сборщик встраивает `.gfont` внутрь JavaScript-пакета, поэтому приложение не запрашивает эти файлы по сети во время работы.
1. Подключить контроллер, не устанавливая ручку над листом.
2. Проверить небольшие перемещения кнопками jog.
3. Проверить `Перо ↑` и `Перо ↓`; при обратном направлении поменять значения местами.
4. Подвести каретку к левому верхнему углу области письма и нажать `Это ноль` (для EBB ноль задаётся положением после сброса).
5. Сначала отправить короткую тестовую строку на малой скорости.
Значения сервопривода зависят от платы. Восстановленные заводские значения — GRBL 12000/18000 и Marlin 50/0 — нельзя считать универсальными.